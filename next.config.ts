import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["example.com", "placehold.co", "avatar.iran.liara.run", "res.cloudinary.com"],
  },
};

export default nextConfig;
